# Frontend moved here

This directory is the new location for the UI/Tauri app that used to
live under `src/desktop-app`.

IMPORTANT: The original source remains at `src/desktop-app` for now to
keep backward compatibility during the refactor. When the team is ready
we can delete the original folder and update any CI scripts.
